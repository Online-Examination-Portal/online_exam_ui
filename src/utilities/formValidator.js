export const formValidators = (type, value = "") => {
  if (type && value) {
    if (type === "") {
      // const exp = /^[0-9]+$/
      // const regex = new RegExp(exp, "g");
      // return regex.test(value);
    }
  }
};
